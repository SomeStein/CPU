Place a prebuilt `c_native.exe` here if you want Windows runs to work without a host compiler.

If the file is absent, the build step falls back to compiling `tsc_benchmark.c` with a compiler found on `PATH`.
